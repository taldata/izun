# Hooks config - simplified for PostgreSQL
# SQLite-specific hooks are no longer needed with RDS

files:
  "/opt/elasticbeanstalk/hooks/appdeploy/pre/01_setup.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      echo "Pre-deployment setup complete"
